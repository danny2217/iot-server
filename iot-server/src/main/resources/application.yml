server:
  address: 0.0.0.0  # 👈 외부(폰)에서 접속 허용 (여기에 적는 겁니다!)
  port: 8080

spring:
  application:
    name: iot-server  # 이건 그냥 두세요! (서버 이름)

  # 여기서부터 DB 설정 (같은 줄 라인에 맞추세요)
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    # 도커(Mac)니까 3307번!
    url: jdbc:mariadb://192.168.0.48:3306/iot_db
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true